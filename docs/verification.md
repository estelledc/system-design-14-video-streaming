# Verification

## Current receipt

Implementation commit `91c773e7f1847bb4bc6f0de231c1b005b31c4cfc` is green in public GitHub Actions
[run 32175254211](https://github.com/estelledc/system-design-14-video-streaming/actions/runs/32175254211). The run completed on
2026-08-19 CST with PostgreSQL 17.6 and exact Node releases 22.23.2, 24.19.0, and 26.7.0.

| Gate | Current result | Evidence boundary |
|---|---|---|
| repository/syntax | pass: 41 files, 20 JavaScript files, 12 Markdown files, 2 pinned actions | artifacts, parsing, links, pins, portable paths, evidence vocabulary |
| pure/filesystem/HTTP tests | 16 passing, 0 failed, 0 skipped on every runtime | bounds, ranges, deterministic segmentation, CAS install/readback, service/HTTP/log contracts |
| generated byte corpora | 120 deterministic trials | lossless coverage, bounds, per-segment/source/manifest digest and canonical form |
| dependency audit | `npm audit --audit-level=high`: 0 vulnerabilities | current npm advisory result, not full supply-chain assurance |
| PostgreSQL integration | 7 passing, 0 failed, 0 skipped on every runtime | creation races, offsets, finalize atomicity, lease fencing, publication, tombstone |
| process smoke | pass on every runtime | response-loss retry, API/worker `SIGKILL`, object reuse, ready invisibility, range, tombstone |
| bounded benchmark | completed on every runtime; raw values below | raw small-fixture timing only |

The first public workflow at implementation commit `c16bfa204e6c50b0f3ab308eb1324883d73ff096` is deliberately retained as
[red run 32175132813](https://github.com/estelledc/system-design-14-video-streaming/actions/runs/32175132813). It created no jobs
because job-level `env` referenced `runner.temp`, while that position is evaluated before the runner context exists. Commit
`91c773e7f1847bb4bc6f0de231c1b005b31c4cfc` removed the unused variable; no product assertion or test was weakened.

## Process and benchmark receipts

Every matrix job reported the same smoke facts: the API and worker exits were both `SIGKILL`; exact chunk replay returned
`created=false` with final committed offset 150,000; recovered attempt was 2; all ready objects were reused; ready remained
invisible before publish; the manifest contained three segments; the range response was `206`; tombstone version was 1; and
decode, playback, and human-view claim counters were all zero.

The benchmark input was fixed at one 524,288-byte synthetic source, 64 sequential 8,192-byte chunks, eight 65,536-byte synthetic
segments, and 500 local 256-byte range reads. These are raw hosted-runner observations, not capacity estimates:

| Node | PostgreSQL | upload chunks/s | finalize ms | materialize ms | range reads/s |
|---|---:|---:|---:|---:|---:|
| 22.23.2 | 17.6 | 300.950 | 17.830 | 43.802 | 1768.716 |
| 24.19.0 | 17.6 | 231.465 | 21.373 | 32.899 | 1230.015 |
| 26.7.0 | 17.6 | 237.270 | 24.201 | 34.452 | 1237.006 |

## Full gate

```bash
npm ci --ignore-scripts
npm run check:ci
```

The full gate runs static repository policy, pure/generated/local-filesystem/HTTP tests, high-severity dependency audit, real
PostgreSQL integration, a true-process crash smoke, and a bounded benchmark on a disposable schema and object directory.

## Executable counterexamples

- Twenty concurrent upload-open requests under one owner/key must create one video/upload; changed intent conflicts.
- Two different chunk keys racing at one offset cannot both advance it; exact winning-key replay returns the first result.
- Correct length with a wrong full digest creates no processing job.
- A recovered worker replaces the lease token; the old worker cannot commit even with an earlier nominal completion time.
- Segment/manifest objects written before a worker exit remain invisible and can be verified/reused after lease recovery.
- A ready rendition returns `404` until an explicit publication transaction selects it.
- Removing one referenced segment makes publication fail integrity and leaves the video ready/invisible.
- Repeated equal segment bytes may share a content object while retaining separate ordered manifest positions.
- A tombstone makes later origin manifest and segment observations return `410` while objects remain physically present.
- Multiple ranges fail before any `server_bytes_written` receipt.

## Process smoke target

The smoke performs this exact sequence with real processes, PostgreSQL, filesystem objects, and loopback HTTP:

1. open an upload;
2. commit chunk 1, then `SIGKILL` the API before its response;
3. restart and exactly replay chunk 1 without duplicate offset advance;
4. complete and finalize the source;
5. claim a job, write every segment/manifest, then `SIGKILL` before ready commit;
6. verify the manifest endpoint is still `404`;
7. recover the expired job with attempt 2, verify/reuse the existing objects, and commit ready;
8. verify ready is still `404`, restart the API, and explicitly publish;
9. fetch a `206` range and observe only `server_bytes_written`;
10. tombstone and observe `410` for both manifest and segment;
11. scan every child-process structured record for fixture credentials, keys, bytes, IDs, digests, and paths.

## What green will not prove

- valid media, codec decode/encode, HLS/DASH, adaptive quality, audio/video continuity, or real playback;
- remote client receipt, durable client storage, screen display, audibility, attention, completion, or a human view;
- multi-host/object-store durability, power-loss behavior, replication, failover, backup restore, or disaster recovery;
- direct/pre-signed/multipart upload, parallelism, upload expiry, orphan collection, or production media sizes;
- CDN distribution, shared caching, signed access, cache purge, post-deletion residual-access bounds, or network performance;
- authentication/account correctness beyond the lab allowlist, private content, moderation, malware safety, copyright ownership,
  DRM, privacy/legal compliance, or physical erasure;
- source-chapter scenario throughput/cost, production capacity, deployment, SLA, or external acceptance.

The smoke will report `decodeClaims: 0`, `playbackClaims: 0`, and `humanViewClaims: 0` deliberately.
